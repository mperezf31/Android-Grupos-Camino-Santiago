<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/group_order" android:checkableBehavior="single">
        <item
            android:id="@+id/order_by_distance"
            android:title="@string/order_by_distance" />
        <item
            android:id="@+id/order_by_departure_date"
            android:title="@string/order_by_date" />
    </group>

</menu>
